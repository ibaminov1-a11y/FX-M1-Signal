# R5 settings, scenario lifecycle and chart history — implementation plan

Goal: implement the approved Scenario Engine v2 and the user's selected account/lot controls; deliver through GitHub Actions.
Base: 052a6bd19aaad40bbb7352551bbd62386cf1dc54 on feature/r4-compute-rebuild. Local ZIP tree verified equals upstream tree 8486c44e765c6f4af32d3dbf1f29ac799be6e2dc.
Spec: docs/superpowers/specs/2026-09-26-scenario-v2.md, plus 26 September instructions: selected account label; fixed lot presets/manual; campaign risk stays. User explicitly instructed inline implementation through GitHub.

Constraints: no live MT5 orders, no REAL execution activation, no loss of event_state, no silent fixed-volume reduction, no gain claims from synthetic testing. Preserve legacy upgrade, Pause/Emergency, full close/history and own-position isolation.

- [x] T1. Tests then fixed-volume enforcement, stable candle clock and durable history/audit store. Files: model.py, risk.py, mt5_adapter.py, store.py.
- [x] T2. Tests then causal structural detectors, scenario event lifecycle, ranking and serialization. Files: scenarios/{structure,lifecycle,core}.py. Scope: triangles, range, channel, wedges, broadening, flags/pennants, multi-extrema, head-and-shoulders. Wave/cup modules remain explicitly research backlog.
- [x] T3. Wire SCENARIO_V2 into Engine, historical API and same-engine offline replay. Preserve legacy campaigns until flat, use immutable forecast IDs and execution records. Tests: no synthetic crossings, dedup, invalidation/close-before-reverse, restart/stale, real gate.
- [ ] T4. Android tests then fixed lot presets/manual, selected/actual account split, live/historical chart viewport, zoom/pan, alternatives and forecast archive. Existing compatibility rendering remains only for saved older maps.
- [ ] T5. GitHub Actions: verify RED assertions on previous implementation, full GREEN Python and Android API35, exact commit and persistent signing. Fail on missing screenshot or test, no CI-only uncommitted production patch.
- [ ] T6. Review output, inspect rendered UI images and archive contents, deliver full APK + Bridge + source with hashes. Market performance/physical phone clearly untested.

Baseline: 176 tests passed with /usr/bin/python3 and local isolated dependencies. Default venv lacked Flask; no product failure. No Android SDK required locally: CI is the build environment.

Progress: T1 RED 6/11 failed on exact intended defects; GREEN 11/11. T2 RED 17/17 missing engine; GREEN 17/17. T3 RED 6/6 missing integration; GREEN 6/6. Full Python suite now 210/210. Android RED execution will run on GitHub before applying production diff.
